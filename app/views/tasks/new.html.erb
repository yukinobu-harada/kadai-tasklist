<h1>タスク新規作成ページ</h1>

<%# <form></form>と同様 model: @taskでnewアクションの@taskを使用することを明示 local: trueは画面遷移を伴う同期通信 %>
<%= form_with(model: @task, local: true) do |f| %>
  <%# f.label,f.text_fieldでどのカラムに対する入力欄なのかを明示 %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>